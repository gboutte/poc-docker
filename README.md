# Blue green php deployement

Start the prod mode
```bash
sh init-prod.sh
```

Start the dev mode
```bash
sh start-dev.sh
```


To build the beta containers
```bash
sh build-beta.sh
```


To switch the beta containers to prod
```bash
sh switch.sh
```

